# Copy this file to ".env" and put your real key there. Never commit ".env".
GEMINI_API_KEY=your_key_here

# Optional: use another model name if AI Studio lists a newer free Flash model
# GEMINI_MODEL=gemini-3.6-flash

# Optional: set to 0 to run rules-only (no AI calls)
# USE_LLM=1
